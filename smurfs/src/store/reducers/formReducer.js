const initialState = {
  name: "",
  age: "",
  height: "",
  id: "",
};

export const formReducer = (state = initialState, action) => {
  switch (action.type) {
    default:
      return state;
  }
};
